<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="androidpro.com.tcc_task">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TCCTASK">
        <activity android:name=".PerfilPrestadorServico"></activity>
        <activity android:name=".PerfilCliente" />
        <activity android:name=".ChatComunicacao" />
        <activity android:name=".SuporteCliente" />
        <activity android:name=".SuportePrestadorServico" />
        <activity android:name=".Localizacao" />
        <activity android:name=".Relatorios" />
        <activity android:name=".CadastroPrestador" />
        <activity android:name=".AnunciosDisponiveis" />
        <activity android:name=".CadastrodosAnuncios" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>